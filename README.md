Image morpher

TODO:
1. Investigate and implement subWindow coords to be reshape automatically (save coefficients of scaling)
2. Invsetigate image size vs catmull subdivision dependence (on large images we see peaks at small morph radius, why doesnt this happen on small images?)
3. Implement tube morphing
4. Research mesh normals rotation, and/or should it be z-coord elevation (how high to elevate? )
